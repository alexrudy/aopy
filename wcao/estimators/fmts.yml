fitting:
  float_peak: true
  initial_alpha: 0.999
  mask_radius: 10
  max_alpha: 1.5
  min_alpha: 0.5
  search_radius: 10
  maxfev: 1000
logging:
  loggers:
    wcao.estimators.fmts.find_and_fit_peak:
      level: CRITICAL